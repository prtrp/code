#include <stdio.h>

int main() {
	
	int x; // declaration
	x = 10; // initialization
	
	int y = x / 2;
	printf("the magin value of number is: %i\nThe vale of x is %i\n", y, x); // we have to declar what type of data we will insert %i = integer	
	return 0;

}
