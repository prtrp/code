#include <stdio.h>

void operators(void);

int main()
{
  // expressions are a sequence of operands and operators; 
  // ex:
  int a = 5    +     5;
  /*    | |    |     |
        |-- operator |
          |          |
          |      operand
      operand 
    
  ----------------------------------
  but there are 3 type of operators: */


  return 0;
}

void unaryOperator()
{
  // is called so because use only 1 operand
  // are:
  // -10 (unary minus) -> for change transofrm positive in negative
  // ++, -- (incrementer and decrementer)
  //
}
