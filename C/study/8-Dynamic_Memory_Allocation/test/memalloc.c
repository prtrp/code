#include <stdio.h>
#include <stdlib.h>

void insert_end()

int main(void) {
  int arr_size = 10;
  int position = 0, value = 12, arr_elements = 0;

  int* arr_num = malloc(arr_size * sizeof(int));
  if(arr_num == NULL){
    printf("error with ")
  }
  for(int i = 0; i < 5; i++){
    arr_num[i] = i + 1;
  }

  void insert_end()  
}
