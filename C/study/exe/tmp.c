#include <stdio.h>
#include <stdlib.h>

int main() {
    char a[] = "zb";

    if(a[0] > a[1]) {
        printf("%c > %c\n", a[0], a[1]);
    }
    else{
        printfb("%c < %c\n", a[0], a[1]);
    }
    return 0;
}

